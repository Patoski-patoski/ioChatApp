<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.7.2/socket.io.min.js"
        integrity="sha512-Xm9qbB6Pu06k3PUwPj785dyTl6oHxgsv9nHp7ej7nCpAqGZT3OZpsELuCYX05DdonFpTlBpXMOxjavIAIUwr0w=="
        crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script defer src="/javascripts/chat.js"></script>

    <style>
        body {
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: #010409;
            color: #9198a1;
        }

        main {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            max-width: 100vw;
            padding: 1rem;
        }

        .form-join {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-bottom: 2rem;
        }

        .form-join input {
            margin: 0.5rem;
            padding: 0.75rem;
            border-radius: 5px;
            border: none;
            background-color: #0d1117;
            color: #9198a1;
        }

        .form-join input:focus {
            outline: none;
        }

        .form-join button {
            background-color: #238636;
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 5px;
            cursor: pointer;
        }

        .form-join button:hover {
            background-color: #2ea043;
        }

        .chat-display {
            list-style: none;
            padding: 0;
            margin: 0;
            width: 100%;
            max-width: 800px;
            background-color: #0d1117;
            border-radius: 10px;
            overflow-y: auto;
            max-height: 400px;
            border: 1px solid #30363d;
        }

        .chat-display li {
            padding: 1rem;
            margin-bottom: 0.5rem;
            background-color: #161b22;
            border-bottom: 1px solid #30363d;
        }

        .chat-display li:nth-child(odd) {
            background-color: #0d1117;
        }

        .form-msg {
            display: flex;
            justify-content: center;
            width: 100%;
            max-width: 800px;
            margin-top: 1rem;
        }

        .form-msg input {
            flex-grow: 1;
            border: none;
            padding: 0.75rem;
            border-radius: 5px;
            margin-right: 1rem;
            background-color: #0d1117;
            color: #9198a1;
        }

        .form-msg input:focus {
            outline: none;
        }

        .form-msg button {
            background-color: #238636;
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 5px;
            cursor: pointer;
        }

        .form-msg button:hover {
            background-color: #2ea043;
        }

        .user-list,
        .room-list,
        .activity {
            margin-top: 1rem;
            color: #9198a1;
        }

        .user-list {
            font-weight: bold;
        }

        .room-list {
            font-style: italic;
        }
    </style>
</head>

<body>
    <main>
        <!-- Form to join the chat -->
        <form class="form-join">
            <input type="text" id="name" maxlength="8" placeholder="Your name" size="8" required readonly value="<%= friendUsername %>">
            <input type="text" id="room" placeholder="Chat room" size="8" required readonly value="<%= uniqueCode %>">
            <button id="join" type="submit">Join</button>
        </form>

        <!-- Display chat messages -->
        <ul class="chat-display"></ul>

        <!-- Additional chat information -->
        <p class="user-list">Active users: <span id="user-count">0</span></p>
        <p class="room-list">Room: <%= uniqueCode %>
        </p>
        <p class="activity">No recent activity</p>

        <!-- Form to send messages -->
        <form class="form-msg">
            <input type="text" id="message" placeholder="Your message" required>
            <button type="submit">Send</button>
        </form>
    </main>
</body>

</html>